#!/bin/sh
# pre-push hook — works on Mac, Linux, and Windows (Git for Windows bash)
# Passes stdin (ref data from git) directly to the Node.js implementation.
HOOK_DIR="$(cd "$(dirname "$0")" && pwd)"
exec node "$HOOK_DIR/pre-push.js"
